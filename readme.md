# ETU004362 - Lancer le projet


## 1. Emplacement des fichiers SQL
- `base.sql` : `./base.sql`
- `donne.sql` : `./donne.sql`

## 2. Import SQL (exemple)
```bash
mysql -u root -p bibliotheque < base.sql
mysql -u root -p bibliotheque < donne.sql
```
